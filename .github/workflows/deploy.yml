name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch: # 允许手动触发

jobs:
  deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 安装 pnpm
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      # 3. 设置 Node.js 环境
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      # 4. 安装依赖
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 5. 构建项目
      - name: Build project
        run: pnpm run build
        env:
          NODE_ENV: production
          VITE_API_BASE_URL: /api
          VITE_SOCKET_URL: https://onespecial.me/ws
          VITE_APP_TITLE: 文档平台

      # 6. 创建部署包
      - name: Create deployment archive
        run: |
          cd dist
          tar -czf ../deploy.tar.gz .
          cd ..

      # 7. 部署到服务器
      - name: Deploy to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          source: "deploy.tar.gz,nginx.conf"
          target: "/tmp/"

      # 8. 解压并移动文件到部署目录
      - name: Extract and move files
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          script: |
            echo "🔍 开始部署流程..."
            echo "📍 部署路径: ${{ secrets.DEPLOY_PATH }}"
            echo "📅 当前时间: $(date)"
            
            # 检查部署路径
            if [ ! -d "${{ secrets.DEPLOY_PATH }}" ]; then
              echo "⚠️  部署路径不存在,将创建..."
            else
              echo "✅ 部署路径存在"
              echo "📂 当前文件列表:"
              ls -lh ${{ secrets.DEPLOY_PATH }}/index.html 2>/dev/null || echo "index.html 不存在"
            fi
            
            # 创建备份
            if [ -d "${{ secrets.DEPLOY_PATH }}" ]; then
              echo "💾 Creating backup..."
              sudo rm -rf ${{ secrets.DEPLOY_PATH }}.backup
              sudo cp -r ${{ secrets.DEPLOY_PATH }} ${{ secrets.DEPLOY_PATH }}.backup
            fi
            
            # 创建部署目录
            sudo mkdir -p ${{ secrets.DEPLOY_PATH }}
            
            # 清空当前部署目录
            echo "🗑️  清空部署目录..."
            sudo rm -rf ${{ secrets.DEPLOY_PATH }}/*
            
            # 检查压缩包
            if [ ! -f "/tmp/deploy.tar.gz" ]; then
              echo "❌ 错误: /tmp/deploy.tar.gz 不存在!"
              exit 1
            fi
            echo "✅ 压缩包存在: $(ls -lh /tmp/deploy.tar.gz)"
            
            # 解压新文件
            echo "📦 解压文件到 ${{ secrets.DEPLOY_PATH }}..."
            cd ${{ secrets.DEPLOY_PATH }}
            sudo tar -xzf /tmp/deploy.tar.gz
            
            # 验证解压结果
            echo "📁 解压后的文件:"
            ls -lh ${{ secrets.DEPLOY_PATH }}/index.html
            
            # 清理临时文件
            rm -f /tmp/deploy.tar.gz
            
            # 设置权限
            echo "🔐 设置文件权限..."
            sudo chown -R www-data:www-data ${{ secrets.DEPLOY_PATH }}
            sudo chmod -R 755 ${{ secrets.DEPLOY_PATH }}
            
            # 更新文件时间戳
            echo "⏰ 更新文件时间戳..."
            sudo find ${{ secrets.DEPLOY_PATH }} -type f -exec touch {} \;
            
            # 清除nginx缓存
            echo "🧹 清除nginx缓存..."
            if [ -d "/var/cache/nginx" ]; then
              sudo rm -rf /var/cache/nginx/*
              echo "✅ nginx缓存已清除"
            fi
            
            # ⭐ 更新 Nginx 配置（新增）
            if [ -f "/tmp/nginx.conf" ]; then
              echo "Updating Nginx configuration..."
              sudo cp /etc/nginx/sites-available/onespecial.me /etc/nginx/sites-available/onespecial.me.backup
              sudo cp /tmp/nginx.conf /etc/nginx/sites-available/onespecial.me
              sudo nginx -t && echo "✅ Nginx config test passed" || echo "❌ Nginx config test failed"
              rm -f /tmp/nginx.conf
            fi
            
            # 重启 Nginx
            echo "🔄 重启nginx..."
            sudo systemctl restart nginx
            
            # 显示最终结果
            echo ""
            echo "=" "=" "=" "=" "=" "=" "=" "=" "=" "="
            echo "📊 部署完成! 最终状态:"
            echo "=" "=" "=" "=" "=" "=" "=" "=" "=" "="
            echo "� 文件信息:"
            ls -lh ${{ secrets.DEPLOY_PATH }}/index.html
            echo ""
            echo "📅 文件修改时间: $(stat -c %y ${{ secrets.DEPLOY_PATH }}/index.html)"
            echo "🌐 nginx状态: $(sudo systemctl is-active nginx)"
            echo ""
            echo "✅ Deployment completed successfully!"

      # 9. 验证部署
      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          script: |
            # 检查文件是否存在
            if [ -f "${{ secrets.DEPLOY_PATH }}/index.html" ]; then
              echo "✅ Deployment verified: index.html exists"
              ls -lh ${{ secrets.DEPLOY_PATH }}/index.html
            else
              echo "❌ Deployment failed: index.html not found"
              exit 1
            fi
            
            # 检查 Nginx 状态
            if sudo systemctl is-active --quiet nginx; then
              echo "✅ Nginx is running"
            else
              echo "❌ Nginx is not running"
              exit 1
            fi

      # 10. 发送通知（可选）
      - name: Deployment notification
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "🎉 Deployment to onespecial.me succeeded!"
          else
            echo "❌ Deployment to onespecial.me failed!"
          fi
